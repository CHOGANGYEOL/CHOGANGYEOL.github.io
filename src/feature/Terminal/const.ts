export type Commands = 'whoami' | 'pwd' | 'exit' | 'ls' | 'cd' | 'clear';

export const COMMANDS: Array<Commands> = ['whoami', 'pwd', 'exit', 'ls', 'cd', 'clear'];

export type Path = 'blog' | 'main' | 'profile' | 'project' | 'terminal' | '~';

export const PATH: Array<Path> = ['blog', 'main', 'profile', 'project', 'terminal', '~'];
